# Ejemplo de Algoritmos: Fibonacci Recursivo
# (Versión simplificada para CI)

funcion fib(n) {
    si n <= 1 {
        retornar n
    }
    retornar fib(n - 1) + fib(n - 2)
}

imprimir("Calculando Fibonacci de 10...")
resultado = fib(10)
imprimir("Resultado:")
imprimir(resultado)
